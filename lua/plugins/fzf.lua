-- Fill the screen when using the fuzzy finder
vim.g['fzf_layout'] = { window = { width = 1.0, height = 1.0 } }

-- Disable file preview
-- Comment the line below to re-enable it
vim.g['fzf_preview_window'] = {}
